#include "conception-orientee-objet/example-adder.hpp"

namespace coo {
auto add(const long first, const long second) -> long { return first + second; }

auto sub(const long first, const long second) -> long { return first - second; }
} // namespace coo
